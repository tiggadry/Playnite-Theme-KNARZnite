<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Style x:Key="DetailsViewItemStyle" TargetType="{x:Type ListBoxItem}">
    <Setter Property="Foreground" Value="{DynamicResource TextBrushDarker}" />
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="FontSize" Value="{StaticResource FontSize}" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListBoxItem}">
          <Grid>
            <Border
              x:Name="HoverRectangle"
              Visibility="Collapsed" IsHitTestVisible="False"
              BorderBrush="{DynamicResource HighlightGlyphBrush}"
              BorderThickness="4,0,0,0" />
            <Border
              x:Name="SelectedRectangle"
              Visibility="Collapsed" IsHitTestVisible="False"
              Background="{DynamicResource GlyphBrush}"
              BorderThickness="0" Opacity="0.15" />
            <Border Padding="{TemplateBinding Padding}" Background="Transparent">
              <ContentPresenter />
            </Border>
          </Grid>
          <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding IsInstalled}" Value="True">
              <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            </DataTrigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Visibility" TargetName="HoverRectangle" Value="Visible" />
              <Setter Property="Visibility" TargetName="SelectedRectangle" Value="Visible" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter Property="BorderBrush" TargetName="HoverRectangle" Value="{DynamicResource GlyphBrush}" />
              <Setter Property="Visibility" TargetName="HoverRectangle" Value="Visible" />
              <Setter Property="Visibility" TargetName="SelectedRectangle" Value="Visible" />
              <Setter Property="Opacity" TargetName="SelectedRectangle" Value="0.30" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
