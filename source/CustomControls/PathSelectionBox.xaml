<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
  xmlns:sys="clr-namespace:System;assembly=mscorlib"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="{x:Type PathSelectionBox}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type TextBox}">
          <DockPanel>
            <Button
              x:Name="PART_ButtonDirectorySelect"
              DockPanel.Dock="Right"
              MaxHeight="{TemplateBinding ActualHeight}"
              Margin="5,0,0,0"
              Content="&#xec5b;"
              ToolTip="{DynamicResource LOCSelectDirectoryTooltip}"
              FontFamily="{StaticResource FontIcoFont}" />
            <Button
              x:Name="PART_ButtonFileSelect"
              DockPanel.Dock="Right"
              MaxHeight="{TemplateBinding ActualHeight}"
              Margin="5,0,0,0"
              Content="&#xef27;"
              ToolTip="{DynamicResource LOCSelectFileTitle}"
              FontFamily="{StaticResource FontIcoFont}" />
            <Border
              x:Name="Border"
              Style="{DynamicResource HighlightBorder}"
              DockPanel.Dock="Left"
              Background="{TemplateBinding Background}"
              BorderBrush="{TemplateBinding BorderBrush}"
              BorderThickness="{TemplateBinding BorderThickness}"
              CornerRadius="{DynamicResource ControlCornerRadius}">
              <ScrollViewer
                x:Name="PART_ContentHost"
                HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"
                VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}" />
            </Border>
          </DockPanel>
          <ControlTemplate.Triggers>
            <Trigger Property="IsFocused" Value="True">
              <Setter Property="BorderBrush" TargetName="Border" Value="{DynamicResource GlyphBrush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
