<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Style TargetType="{x:Type LibraryDetailsView}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type LibraryDetailsView}">
          <DockPanel>
            <Grid DockPanel.Dock="{Settings DetailsListPosition}" Width="{Settings DetailsListWitdh}">
              <ExtendedListBox
                x:Name="PART_ListGames"
                BorderBrush="{DynamicResource PanelSeparatorBrush}"
                ItemContainerStyle="{DynamicResource DetailsViewItemStyle}"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.PanningMode="VerticalOnly"
                VirtualizingStackPanel.VirtualizationMode="Recycling">
                <ExtendedListBox.Style>
                  <Style BasedOn="{StaticResource {x:Type ExtendedListBox}}" TargetType="ExtendedListBox">
                    <Setter Property="BorderThickness" Value="0,0,1,0" />
                    <Style.Triggers>
                      <DataTrigger Binding="{Settings DetailsListPosition}" Value="Right">
                        <Setter Property="BorderThickness" Value="1,0,0,0" />
                      </DataTrigger>
                      <DataTrigger Binding="{Settings ShowPanelSeparators}" Value="False">
                        <Setter Property="BorderThickness" Value="0" />
                      </DataTrigger>
                    </Style.Triggers>
                  </Style>
                </ExtendedListBox.Style>
                <ExtendedListBox.GroupStyle>
                  <GroupStyle ContainerStyle="{StaticResource DetailsViewGroupStyle}" />
                </ExtendedListBox.GroupStyle>
                <ExtendedListBox.ItemTemplate>
                  <DataTemplate>
                    <GameListItem Style="{StaticResource DetailsViewItemTemplate}" />
                  </DataTemplate>
                </ExtendedListBox.ItemTemplate>
              </ExtendedListBox>
              <TextBlock
                Margin="0,15,0,0" HorizontalAlignment="Center"
                Text="{DynamicResource LOCNoItemsFound}"
                FontSize="{StaticResource FontSizeLarge}"
                TextAlignment="Center" TextWrapping="Wrap">
                <TextBlock.Style>
                  <Style BasedOn="{StaticResource BaseTextBlockStyle}" TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                      <DataTrigger Binding="{Binding Items.Count, ElementName=PART_ListGames, Mode=OneWay}" Value="0">
                        <Setter Property="Visibility" Value="Visible" />
                      </DataTrigger>
                    </Style.Triggers>
                  </Style>
                </TextBlock.Style>
              </TextBlock>
            </Grid>
            <DetailsViewGameOverview x:Name="PART_ControlGameView" DockPanel.Dock="Left" />
          </DockPanel>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="DetailsViewTabHeaderStyle" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{DynamicResource TextBrushDarker}" />
    <Setter Property="FontWeight" Value="Bold" />
    <Setter Property="FontSize" Value="{DynamicResource FontSizeLarger}" />
    <Setter Property="Typography.Capitals" Value="AllSmallCaps" />
    <Style.Triggers>
      <DataTrigger Binding="{Binding Path=IsSelected, RelativeSource={RelativeSource AncestorType={x:Type TabItem}}}" Value="True">
        <Setter Property="Foreground" Value="{DynamicResource GlyphBrush}" />
      </DataTrigger>
      <MultiDataTrigger>
        <MultiDataTrigger.Conditions>
          <Condition Binding="{Binding Path=IsSelected, RelativeSource={RelativeSource AncestorType={x:Type TabItem}}}" Value="False" />
          <Condition Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
        </MultiDataTrigger.Conditions>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
      </MultiDataTrigger>
    </Style.Triggers>
  </Style>
</ResourceDictionary>
