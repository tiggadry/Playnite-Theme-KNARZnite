<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Style TargetType="{x:Type ExplorerPanel}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ExplorerPanel}">
          <Border BorderBrush="{DynamicResource PanelSeparatorBrush}">
            <Border.Style>
              <Style TargetType="Border">
                <Setter Property="BorderThickness" Value="1,0,0,0" />
                <Style.Triggers>
                  <DataTrigger Binding="{Settings ExplorerPanelPosition}" Value="Left">
                    <Setter Property="BorderThickness" Value="0,0,1,0" />
                  </DataTrigger>
                  <DataTrigger Binding="{Settings ShowPanelSeparators}" Value="False">
                    <Setter Property="BorderThickness" Value="0" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Border.Style>
            <DockPanel Background="{TemplateBinding Background}">
              <DockPanel
                DockPanel.Dock="Top"
                Margin="5,8,5,8"
                LastChildFill="True">
                <Button
                  Style="{StaticResource SimpleButton}"
                  DockPanel.Dock="Right"
                  Margin="0,0,5,0" VerticalAlignment="Center"
                  Content="r"
                  FontFamily="Marlett" Name="PART_ButtonClose" />
                <ComboBox
                  DockPanel.Dock="Left"
                  Margin="0,0,8,0"
                  MaxDropDownHeight="Auto" Name="PART_SelectFields" />
              </DockPanel>
              <ListBox
                DockPanel.Dock="Top"
                Margin="5,0,5,8"
                Background="Transparent" BorderThickness="0"
                Name="PART_SelectItems" ScrollViewer.HorizontalScrollBarVisibility="Disabled" />
            </DockPanel>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
