<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Style TargetType="{x:Type NotificationPanel}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="NotificationPanel">
          <DockPanel Background="{DynamicResource PopupBackgroundBrush}">
            <DockPanel DockPanel.Dock="Top" Margin="13,15,7,15">
              <Button
                Style="{StaticResource SimpleButton}"
                DockPanel.Dock="Right"
                Margin="0,0,5,0"
                Content="r"
                FontFamily="Marlett" Name="PART_ButtonClose" />
              <Button
                Style="{StaticResource SimpleButton}"
                DockPanel.Dock="Left"
                HorizontalAlignment="Left"
                Content="{DynamicResource LOCDismissAll}"
                Name="PART_ButtonDismissAll" />
            </DockPanel>
            <ItemsControl DockPanel.Dock="Top" Name="PART_ItemsMessages">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Button Style="{StaticResource NotificationMessage}" />
                </DataTemplate>
              </ItemsControl.ItemTemplate>
              <ItemsControl.Template>
                <ControlTemplate>
                  <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <ItemsPresenter />
                  </ScrollViewer>
                </ControlTemplate>
              </ItemsControl.Template>
            </ItemsControl>
          </DockPanel>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
